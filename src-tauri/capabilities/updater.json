{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Auto-update calls, main window only: the quick-add window has no UI to update from and no business restarting the app.",
  "windows": [
    "main"
  ],
  "permissions": [
    "updater:default",
    "process:default"
  ]
}
